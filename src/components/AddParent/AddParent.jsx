import React, { memo } from 'react'

import { Input } from '@/components/UI'
import Flex from '@/components/Flex'

export default memo(() => {
    return (
        <Flex>
            {/* <Input placeholder={} */}
            />
        </Flex>
    )
})